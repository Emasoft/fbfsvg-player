# Quick Fix: Add SBOM Generation and macOS x64 Support to Release Workflow
Generated: 2026-01-24 22:56:06

## Change Made
- File: `.github/workflows/release.yml`
- Lines: Multiple sections updated
- Changes:
  1. Added macOS x64 (macos-13) to build matrix
  2. Added macOS x64 Skia and player build steps
  3. Added macOS x64 tarball packaging
  4. Added SBOM generation (SPDX + CycloneDX formats) after package step
  5. Updated release body to include macOS x64 downloads and SBOM section
  6. Added macOS x64 validation in validate job
  7. Updated Homebrew formula to support both ARM64 and x64 architectures
  8. Added macOS x64 checksum fetch in update-packages job

## Verification
- Syntax check: PASS (YAML structure maintained)
- Pattern followed: Existing workflow structure and naming conventions

## Files Modified
1. `.github/workflows/release.yml` - Added SBOM generation, macOS x64 support, and updated package manifests

## Notes
- SBOM files will be generated using anchore/sbom-action@v0 in both SPDX and CycloneDX formats
- macOS x64 builds will run on macos-13 runners
- Homebrew formula now uses `on_arm` and `on_intel` blocks for architecture-specific builds
- All validation and publish steps updated to include new assets
